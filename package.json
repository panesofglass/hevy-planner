{
  "name": "hevy-planner",
  "version": "1.0.0",
  "description": "A training companion for Hevy that adds queue-based scheduling, automatic reflow, multi-phase roadmaps, benchmark tracking, and an open program schema.",
  "scripts": {
    "dev": "wrangler dev",
    "test": "vitest run",
    "test:watch": "vitest",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute hevy-planner --local --file=migrations/0001_initial.sql"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.13.3",
    "@cloudflare/workers-types": "^4.20260317.1",
    "typescript": "^5.9.3",
    "vitest": "^4.1.0",
    "wrangler": "^4.76.0"
  },
  "dependencies": {
    "ajv": "^8.18.0",
    "ajv-formats": "^3.0.1"
  }
}
